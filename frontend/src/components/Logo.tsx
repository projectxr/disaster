import React from 'react';
import { Siren } from 'lucide-react';

const Logo: React.FC = () => {
	return (
		<div className='flex items-center gap-2'>
			<img src='/logo.png' />
		</div>
	);
};

export default Logo;
